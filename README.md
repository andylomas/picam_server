### PiCamServer

Example of a webserver using Python to take pictures
on a Respberry Pi using the Pi Camera.

A simple p5.js sketch is used to create a web app to
send messages to the server to take pictures, request
the images from the server, and display them in a browser

Uses aiohttp and python_socketio
